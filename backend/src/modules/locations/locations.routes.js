/**
 * locations.routes.js — Location / GPS routes.
 * GET /api/locations/geocode   — address → coordinates
 * GET /api/locations/reverse   — coordinates → address
 *
 * Implementation in Phase 7 (Maps and GPS).
 */
import { Router } from 'express'

const router = Router()

export default router
