# docker-compose.yml
# Docker Compose configuration will be fully defined in a later phase.
# Placeholder created to reserve the file in version control.

version: '3.9'

services:
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"

  # backend:
  #   build: ./backend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - postgres

  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: ${DB_USER}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_DB: ${DB_NAME}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
