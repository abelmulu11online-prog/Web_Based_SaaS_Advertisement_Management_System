# components/common/

Composite components that are shared across features but carry some shared logic
(e.g. LoadingSpinner, ErrorBoundary, ProtectedRoute, ImageWithFallback).

Sits between pure UI primitives (ui/) and feature-specific components (features/).
