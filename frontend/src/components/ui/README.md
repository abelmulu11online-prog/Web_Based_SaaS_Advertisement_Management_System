# components/ui/

Pure, stateless UI primitives — buttons, inputs, badges, cards, modals, etc.

Rules:
- No business logic
- No API calls
- Fully controlled via props
- Must be reusable across any feature
